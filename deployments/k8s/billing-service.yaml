apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: billing-backend 
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
  - name: webhook
    port: 8081
    targetPort: 8081
